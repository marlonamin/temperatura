# Definición de la función para calcular la temperatura promedio
def calcular_temperatura_promedio(datos_temperatura):
    """
    Calcula la temperatura promedio de cada ciudad a partir de los datos proporcionados.
    :param datos_temperatura: Diccionario donde las claves son los nombres de las ciudades
                              y los valores son listas con temperaturas semanales.
    :return: Diccionario con la temperatura promedio de cada ciudad.
    """
    promedios = {}
    
    for ciudad, temperaturas in datos_temperatura.items():
        promedios[ciudad] = sum(temperaturas) / len(temperaturas)
    
    return promedios

# Datos de temperatura para 3 ciudades durante 4 semanas
datos_temperatura = {
    "Ciudad A": [22, 24, 26, 23],
    "Ciudad B": [18, 20, 21, 19],
    "Ciudad C": [30, 32, 29, 31]
}

# Llamada a la función y almacenamiento de los resultados
promedios = calcular_temperatura_promedio(datos_temperatura)
